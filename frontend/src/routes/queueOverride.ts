/* The /onboard switches hand the recomputed queue to /maya through the
   session, so demo beat 8 (flip a switch, the queue re-ranks) survives a
   route change. */
export const OVERRIDE_KEY = 'tano.queue.override'
